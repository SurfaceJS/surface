{
    "author": "HitalloExiled",
    "description": "Surface workspace",
    "devDependencies": {
        "@commitlint/cli": "~17.1.1",
        "@commitlint/config-conventional": "~17.1.0",
        "@typescript-eslint/eslint-plugin": "~5.35.1",
        "@typescript-eslint/parser": "~5.35.1",
        "c8": "~7.12.0",
        "eslint": "~8.23.0",
        "eslint-plugin-import": "~2.26.0",
        "husky": "~8.0.1",
        "mocha": "~10.0.0",
        "rimraf": "~3.0.2",
        "tslib": "~2.4.0",
        "typescript": "~4.7.4"
    },
    "keywords": [
        "types",
        "tests",
        "shared"
    ],
    "license": "MIT",
    "name": "@surface/workspace",
    "repository": {
        "type": "git",
        "url": "git+https://github.com/SurfaceJS/surface.git"
    },
    "scripts": {
        "build": "npm run ts:build",
        "cover": "c8 --check-coverage npm test",
        "cover-summary": "c8 --reporter=text-summary --check-coverage npm run test-summary",
        "lint": "eslint . --cache --ext .ts",
        "postinstall": "npm run ts:build",
        "postpublishworkspace": "npm run build",
        "prepare": "husky install .husky",
        "prepublishworkspace": "tasks build-release",
        "publisher:bump": "publisher bump patch --log-level=trace --dry --synchronize --update-file-references --packages=packages/builder",
        "publisher:changed": "publisher changed next --ignore-changes=*.md",
        "publisher:publish": "publisher publish --log-level=trace --dry",
        "publisher:publish-canary": "publisher publish --log-level=trace --dry --canary",
        "publisher:unpublish": "publisher unpublish --log-level=trace --dry",
        "reinstall": "npm run clean && npm i",
        "test": "mocha --loader=@surface/mock-loader --require=./tasks/polyfill.js **/packages/*/tests/**/*.spec.js",
        "test-summary": "npm test -- --reporter=progress",
        "ts:build": "tsc --build",
        "ts:clean": "tsc --build --clean",
        "ts:dry": "tsc --build --clean --dry",
        "ts:rebuild": "tsc --build --clean && tsc --build",
        "ts:watch": "tsc --build --watch"
    },
    "type": "module",
    "version": "1.0.0-beta.0",
    "workspaces": [
        "./packages/*",
        "./tasks"
    ]
}
